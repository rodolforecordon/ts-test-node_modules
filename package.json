{
  "name": "ts-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts",
    "start:dev": "tsx watch index.ts",
    "build": "tsup index.ts",
    "postbuild": "cp package*.json dist && cd dist && npm ci --production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^6.6.3",
    "tsx": "^3.12.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "uuidv4": "^6.2.13"
  }
}
